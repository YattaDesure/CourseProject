# 🚀 Пошаговая инструкция: Деплой бэкенда на TimeWeb

## 📋 Подготовка

**Информация о проекте:**
- Git репозиторий: `https://github.com/YattaDesure/Site.git`
- Ветка: `main`
- Коммит: `483e7e2588ba931fc82fb8322bff6274615d4d01`
- Тип: Backend (ASP.NET Core)
- Фреймворк: Docker
- Build context: `backend/`
- Dockerfile: `backend/Dockerfile`

---

## ШАГ 1: Добавление VCS провайдера (если еще нет)

1. **Зайдите в панель TimeWeb Cloud**
   - Откройте https://timeweb.cloud
   - Войдите в свой аккаунт

2. **Перейдите в раздел "VCS провайдеры"**
   - В меню слева найдите "VCS" или "Git"
   - Или в разделе "Приложения" → "VCS провайдеры"

3. **Нажмите кнопку "Добавить провайдер"** (или "+")

4. **Выберите тип провайдера:**
   - Выберите **"Git"** (для публичных репозиториев)
   - Или **"GitHub"** если хотите подключить через GitHub API

5. **Заполните форму:**
   - **URL репозитория:** `https://github.com/YattaDesure/Site.git`
   - **Логин:** (оставьте пустым для публичных репозиториев)
   - **Пароль/Токен:** (оставьте пустым для публичных репозиториев)

6. **Нажмите "Сохранить"** или "Добавить"

7. **Дождитесь подключения** (обычно несколько секунд)

---

## ШАГ 2: Создание Backend приложения

1. **Перейдите в раздел "Приложения"**
   - В меню слева найдите "Приложения" или "Apps"
   - Нажмите на него

2. **Нажмите кнопку "Создать приложение"** (обычно справа вверху, зеленая кнопка)

3. **Выберите тип приложения:**
   - Выберите **"Backend"** (бэкенд)

4. **Настройка источника кода:**
   - **VCS провайдер:** Выберите тот, который добавили в Шаге 1
   - **Реопозиторий:** Выберите `Site` из списка
   - **Ветка:** Выберите `main`
   - **Коммит:** Выберите последний коммит или введите `483e7e2588ba931fc82fb8322bff6274615d4d01`

5. **Выберите пресет:**
   - Найдите и выберите **"Docker"** или **"ASP.NET Core"**
   - Если есть пресет "Docker" - выберите его
   - Если нет - выберите "Custom" или "Docker Compose"

6. **Нажмите "Далее"** или "Продолжить"

---

## ШАГ 3: Настройка сборки

1. **Build context (контекст сборки):**
   - В поле "Build context" введите: **`backend/`**
   - Это папка, где находится Dockerfile

2. **Dockerfile:**
   - В поле "Dockerfile" введите: **`backend/Dockerfile`**
   - Или оставьте автоматическое определение

3. **Build команда:**
   - Оставьте пустым (для Docker не нужна)
   - Или если есть поле - оставьте по умолчанию

4. **Run команда:**
   - Оставьте пустым (команда в Dockerfile)
   - Dockerfile уже содержит `ENTRYPOINT ["dotnet", "GreenQuarter.Api.dll"]`

5. **Порт:**
   - Укажите **`8080`** (порт из Dockerfile)
   - Или оставьте по умолчанию

6. **Нажмите "Далее"**

---

## ШАГ 4: Переменные окружения

**ВАЖНО:** Добавьте все эти переменные! Без них приложение не запустится.

1. **Найдите раздел "Переменные окружения"** или "Environment Variables"

2. **Нажмите "Добавить переменную"** для каждой:

### Переменные для базы данных:

**DB_HOST:**
- **Имя:** `DB_HOST`
- **Значение:** Адрес вашей БД (если на ноутбуке - URL из ngrok/cloudflared)
  - Пример для ngrok: `0.tcp.ngrok.io`
  - Пример для cloudflared: `0.tcp.eu.ngrok.io`
  - Или IP адрес вашего ноутбука (если доступен)

**DB_PORT:**
- **Имя:** `DB_PORT`
- **Значение:** Порт БД (обычно `1433` для SQL Server)

**DB_NAME:**
- **Имя:** `DB_NAME`
- **Значение:** `GreenQuarter` (имя вашей базы данных)

**DB_USER:**
- **Имя:** `DB_USER`
- **Значение:** `sa` (или ваш пользователь БД)

**DB_PASSWORD:**
- **Имя:** `DB_PASSWORD`
- **Значение:** Ваш пароль от БД (например, `22332123Yaz`)

### Переменные для JWT:

**JWT_KEY:**
- **Имя:** `JWT_KEY`
- **Значение:** Секретный ключ (минимум 32 символа)
  - Пример: `YourSuperSecretKeyThatIsAtLeast32CharactersLong!`
  - **ВАЖНО:** Используйте сложный ключ для продакшена!

**JWT_ISSUER:**
- **Имя:** `JWT_ISSUER`
- **Значение:** `GreenQuarter` (или ваш домен)

**JWT_AUDIENCE:**
- **Имя:** `JWT_AUDIENCE`
- **Значение:** `GreenQuarterUsers` (или ваш домен)

### Переменные для CORS (опционально):

**AllowedOrigins:**
- **Имя:** `AllowedOrigins`
- **Значение:** URL вашего фронтенда (через запятую, если несколько)
  - Пример: `https://ваш-домен-frontend.timeweb.cloud,https://ваш-домен.com`
  - Пока можно оставить пустым, добавите после деплоя фронтенда

3. **Проверьте все переменные** - они должны быть добавлены

4. **Нажмите "Далее"** или "Сохранить"

---

## ШАГ 5: Название и описание

1. **Название приложения:**
   - Введите: **`greenquarter-api`** или **`backend-api`**
   - Или любое другое понятное название

2. **Описание (опционально):**
   - Можно оставить пустым
   - Или написать: "Green Quarter API Backend"

3. **Нажмите "Создать"** или "Деплой"

---

## ШАГ 6: Ожидание сборки

1. **Дождитесь начала сборки**
   - Вы увидите процесс сборки в реальном времени
   - Обычно занимает 5-10 минут

2. **Следите за логами:**
   - Если есть ошибки - проверьте логи
   - Частые проблемы:
     - Неправильный Build context
     - Отсутствующие переменные окружения
     - Ошибки в Dockerfile

3. **После успешной сборки:**
   - Вы получите URL вашего API
   - Обычно это: `https://ваш-проект-api.timeweb.cloud`
   - Или кастомный домен, если настроили

---

## ШАГ 7: Проверка работы

1. **Скопируйте URL API** из панели TimeWeb

2. **Проверьте Swagger:**
   - Откройте в браузере: `https://ваш-url-api.timeweb.cloud/swagger`
   - Должен открыться Swagger UI

3. **Проверьте здоровье API:**
   - Попробуйте сделать запрос: `GET https://ваш-url-api.timeweb.cloud/api/health`
   - Или любой другой endpoint

---

## ⚠️ Важные моменты

### База данных на ноутбуке:

Если база данных на вашем ноутбуке:

1. **Запустите туннель:**
   ```bash
   ./start-db-tunnel.sh
   ```
   Или вручную:
   ```bash
   ngrok tcp 1433
   ```
   Или:
   ```bash
   cloudflared tunnel --url tcp://localhost:1433
   ```

2. **Скопируйте URL туннеля** (например: `0.tcp.ngrok.io:12345`)

3. **Обновите переменные окружения:**
   - `DB_HOST` = адрес из туннеля (например: `0.tcp.ngrok.io`)
   - `DB_PORT` = порт из туннеля (например: `12345`)

4. **Перезапустите приложение** в TimeWeb после обновления переменных

### Если сборка не удалась:

1. **Проверьте Build context:**
   - Должно быть: `backend/`
   - Не `./backend/` или `/backend/`

2. **Проверьте Dockerfile:**
   - Должен быть по пути: `backend/Dockerfile`
   - Проверьте, что он существует в репозитории

3. **Проверьте переменные окружения:**
   - Все обязательные переменные должны быть заполнены
   - Особенно `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD`

4. **Проверьте логи сборки:**
   - В панели TimeWeb найдите раздел "Логи" или "Build logs"
   - Ищите ошибки компиляции или Docker

---

## 📝 После успешного деплоя

1. **Скопируйте URL API** (например: `https://greenquarter-api.timeweb.cloud`)

2. **Обновите переменную окружения фронтенда:**
   - В настройках Frontend приложения добавьте:
   - `VITE_API_URL=https://ваш-url-api.timeweb.cloud`

3. **Пересоберите фронтенд** с новой переменной

4. **Готово!** 🎉

---

## 🆘 Если что-то пошло не так

1. Проверьте логи в панели TimeWeb
2. Убедитесь, что все переменные окружения заполнены
3. Проверьте, что туннель к БД запущен (если БД на ноутбуке)
4. Проверьте, что Build context указан правильно: `backend/`

---

**Удачи с деплоем! 🚀**

