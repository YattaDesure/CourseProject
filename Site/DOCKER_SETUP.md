# 🐳 Docker Compose - Запуск всего проекта

## 🚀 Быстрый старт

### Запуск всех сервисов:

```bash
docker compose up -d
```

### Остановка:

```bash
docker compose down
```

### Просмотр логов:

```bash
docker compose logs -f
```

### Пересборка после изменений:

```bash
docker compose up -d --build
```

---

## 📋 Что запускается:

1. **База данных (SQL Server)**
   - Порт: `1433`
   - Пароль SA: `22332123Yaz`
   - База данных: `GreenQuarter`

2. **Backend API**
   - Порт: `5001`
   - URL: `http://localhost:5001`
   - Swagger: `http://localhost:5001/swagger`

3. **Frontend**
   - Порт: `80`
   - URL: `http://localhost`
   - API проксируется через Nginx

---

## 🔧 Настройка

### Изменить пароль БД:

В `docker-compose.yml` измените:
```yaml
environment:
  - SA_PASSWORD=ваш_новый_пароль
```

И обновите строку подключения в `backend` сервисе.

### Изменить порты:

В `docker-compose.yml` измените:
```yaml
ports:
  - "ваш_порт:5001"  # для backend
  - "ваш_порт:80"    # для frontend
```

---

## 🗄️ База данных

База данных создается автоматически при первом запуске.

Подключение:
- Host: `localhost` (или `db` изнутри Docker сети)
- Port: `1433`
- User: `SA`
- Password: `22332123Yaz`
- Database: `GreenQuarter`

---

## ✅ Готово!

После запуска `docker compose up -d` все будет работать автоматически!

---

**Удобной разработки! 🎉**

