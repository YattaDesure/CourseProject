# 🐳 Запуск в Docker Desktop

## 📋 Способ 1: Через Docker Desktop GUI

### Шаг 1: Откройте Docker Desktop
- Запустите Docker Desktop приложение

### Шаг 2: Добавьте проект
1. В Docker Desktop нажмите на **"Compose"** в левом меню
2. Нажмите **"Open"** или **"Add"**
3. Выберите папку проекта: `/Users/yattadesure/Labs/Site`
4. Docker Desktop автоматически найдет `docker-compose.yml`

### Шаг 3: Запуск
1. Нажмите кнопку **"Start"** или **"Up"**
2. Дождитесь сборки (3-5 минут при первом запуске)

---

## 📋 Способ 2: Через терминал (проще)

### Запуск:
```bash
cd /Users/yattadesure/Labs/Site
docker compose up -d
```

### После запуска:
- Откройте Docker Desktop
- В разделе **"Containers"** вы увидите все запущенные контейнеры:
  - `greenquarter-db` - база данных
  - `greenquarter-api` - бэкенд
  - `greenquarter-frontend` - фронтенд

---

## 🎯 Управление через Docker Desktop

### Просмотр логов:
1. Откройте Docker Desktop
2. Выберите контейнер (например, `greenquarter-api`)
3. Нажмите на вкладку **"Logs"**

### Остановка:
1. Выберите все контейнеры проекта
2. Нажмите **"Stop"** или **"Down"**

### Перезапуск:
1. Выберите контейнер
2. Нажмите **"Restart"**

---

## 🌐 Доступ к приложению

После запуска:
- **Frontend:** http://localhost
- **Backend:** http://localhost:5001
- **Swagger:** http://localhost:5001/swagger

---

## ✅ Готово!

Проект будет виден в Docker Desktop и управляется через GUI или команды.

