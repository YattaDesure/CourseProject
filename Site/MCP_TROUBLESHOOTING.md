# Решение проблем с TimeWeb MCP Server

## ✅ Проверка токена

Токен проверен и выглядит валидным:
- **Формат:** JWT ✅
- **Тип:** api_key ✅
- **Выдан:** 30 ноября 2025 ✅
- **Статус:** Не истек ✅

## ❌ Проблема: Ошибка 401

Получаем ошибку 401 при попытке использовать MCP функции.

## 🔧 Решения

### 1. Перезапуск Cursor (ОБЯЗАТЕЛЬНО)

MCP серверы загружаются только при запуске Cursor. После изменения `.cursor/mcp.json`:

1. **Полностью закройте Cursor** (не просто окно, а приложение)
2. **Откройте Cursor заново**
3. **Подождите 10-15 секунд** для инициализации MCP серверов
4. **Проверьте статус MCP** в настройках Cursor

### 2. Проверка прав токена

Убедитесь, что токен имеет права на:
- Управление приложениями
- Доступ к VCS провайдерам
- Создание и редактирование ресурсов

**Как проверить:**
1. Зайдите в панель TimeWeb
2. Перейдите в раздел API / Настройки разработчика
3. Проверьте права вашего API ключа
4. При необходимости создайте новый токен с полными правами

### 3. Проверка установки MCP сервера

Убедитесь, что `timeweb-mcp-server` доступен:

```bash
npx timeweb-mcp-server --version
```

Если не работает, попробуйте установить глобально:

```bash
npm install -g timeweb-mcp-server
```

### 4. Проверка логов Cursor

1. Откройте настройки Cursor
2. Найдите раздел "MCP" или "Extensions"
3. Проверьте логи на наличие ошибок
4. Ищите сообщения о `timeweb-mcp-server`

### 5. Альтернативная проверка токена

Попробуйте использовать токен напрямую через curl:

```bash
TOKEN="ваш_токен"
curl -H "Authorization: Bearer $TOKEN" \
     https://api.timeweb.cloud/api/v1/vcs-providers
```

Если это работает, значит проблема в MCP сервере, а не в токене.

### 6. Создание нового токена

Если ничего не помогает:

1. Зайдите в панель TimeWeb
2. Удалите старый API ключ
3. Создайте новый с полными правами
4. Обновите `.cursor/mcp.json`
5. Перезапустите Cursor

## 📋 Чек-лист

- [ ] Cursor полностью перезапущен после изменения конфигурации
- [ ] Токен имеет правильный формат (JWT)
- [ ] Токен не истек
- [ ] Токен имеет нужные права в панели TimeWeb
- [ ] `timeweb-mcp-server` доступен через npx
- [ ] Нет ошибок в логах Cursor
- [ ] Токен работает через curl (опционально)

## 🆘 Если ничего не помогает

Используйте альтернативный способ деплоя:
- Через веб-интерфейс TimeWeb (см. `DEPLOY_QUICKSTART.md`)
- Через Docker и ручную настройку (см. `DEPLOY_TIMEWEB.md`)

