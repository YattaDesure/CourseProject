# 🔧 Исправление проблемы с Dockerfile на TimeWeb

## ❌ Проблема

Ошибка в логе:
```
MSBUILD : error MSB1003: Specify a project or solution file. 
The current working directory does not contain a project or solution file.
```

TimeWeb использует неправильный Dockerfile, который:
- Использует `WORKDIR /app`
- Выполняет `RUN dotnet restore` без указания проекта

## ✅ Решение

### Вариант 1: Правильный Build context (РЕКОМЕНДУЕТСЯ)

**В настройках приложения TimeWeb:**

1. **Build context:** `backend/`
   - НЕ `.` (корень)
   - НЕ `./backend/`
   - ТОЧНО: `backend/`

2. **Dockerfile path:** `Dockerfile`
   - Или `backend/Dockerfile` (если context = `.`)

3. **Убедитесь, что НЕ выбран автоматический Dockerfile:**
   - В настройках сборки должна быть опция "Использовать Dockerfile из репозитория"
   - НЕ "Автоматически генерировать Dockerfile"

### Вариант 2: Dockerfile в корне

Если Вариант 1 не работает, используйте `Dockerfile.backend`:

**В настройках приложения TimeWeb:**

1. **Build context:** `.` (корень проекта)

2. **Dockerfile path:** `Dockerfile.backend`

3. **Сохраните и пересоберите**

## 🔍 Как проверить, какой Dockerfile используется

В логах сборки TimeWeb ищите строки:
- `WORKDIR /app` - неправильный (автоматический)
- `WORKDIR /src` - правильный (наш)

## 📝 Что создано

1. **`backend/Dockerfile`** - правильный Dockerfile (обновлен)
2. **`Dockerfile.backend`** - альтернативный для корня проекта

## ⚠️ Важные моменты

1. **Build context критичен!** Если указан неправильно, Dockerfile не найдет файлы проекта
2. **Автоматический Dockerfile** - TimeWeb может генерировать свой, если не найдет правильный
3. **Проверьте настройки** перед каждой сборкой

## 🎯 Пошаговая инструкция

1. Зайдите в настройки приложения в TimeWeb
2. Найдите раздел "Сборка" или "Build settings"
3. Проверьте:
   - Build context: `backend/`
   - Dockerfile: `Dockerfile`
   - Автоматический Dockerfile: **ВЫКЛЮЧЕН**
4. Сохраните настройки
5. Запустите сборку заново

## 🆘 Если ничего не помогает

1. Удалите приложение в TimeWeb
2. Создайте заново с правильными настройками с самого начала
3. Убедитесь, что выбран тип "Docker" или "Backend", а не автоматический

