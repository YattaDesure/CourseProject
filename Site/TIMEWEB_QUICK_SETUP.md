# ⚡ Быстрая настройка для TimeWeb (без Docker)

## 🎯 Что ввести в форму TimeWeb:

### Основные настройки:
1. **Окружение:** `.NET` ✅
2. **Фреймворк:** `ASP.NET` ✅
3. **Версия окружения:** `9.0` ✅

### Команды:

**Команда сборки:**
```
cd backend && dotnet restore GreenQuarter.sln && dotnet build GreenQuarter.Api/GreenQuarter.Api.csproj -c Release
```

**Команда запуска:**
```
dotnet run --project backend/GreenQuarter.Api/GreenQuarter.Api.csproj --urls "http://0.0.0.0:8080"
```

**Путь проверки состояния:**
```
/swagger
```

**Зависимости:**
```
(оставьте пустым)
```

---

## 🔧 Переменные окружения (ОБЯЗАТЕЛЬНО добавить!):

### База данных:
```
DB_HOST = адрес_из_ngrok (например: 0.tcp.ngrok.io)
DB_PORT = порт_из_туннеля (например: 12345)
DB_NAME = GreenQuarter
DB_USER = sa
DB_PASSWORD = 22332123Yaz
```

### JWT:
```
JWT_KEY = YourSuperSecretKeyThatIsAtLeast32CharactersLong!
JWT_ISSUER = GreenQuarter
JWT_AUDIENCE = GreenQuarterUsers
```

### CORS (после деплоя фронтенда):
```
AllowedOrigins = https://ваш-домен-frontend.timeweb.cloud
```

---

## ✅ Готово!

После сохранения настроек и добавления переменных окружения:
1. Нажмите "Создать" или "Деплой"
2. Дождитесь завершения сборки
3. Проверьте Swagger: `https://ваш-url-api.timeweb.cloud/swagger`

---

**Подробная инструкция:** `TIMEWEB_NATIVE_DOTNET.md`

