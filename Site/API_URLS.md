# URL API бэкенда

## 🔵 Локальная разработка

**URL:** `http://localhost:5001`

**Для фронтенда (переменная окружения):**
```bash
VITE_API_URL=http://localhost:5001
```

**Проверка работы:**
- Swagger UI: `http://localhost:5001/swagger`
- Health check: `http://localhost:5001/api/health` (если есть)

## 🟢 Продакшн (TimeWeb Cloud)

После деплоя бэкенда на TimeWeb, URL будет одним из вариантов:

### Вариант 1: Автоматический домен TimeWeb
```
https://ваш-проект-api.timeweb.cloud
```

### Вариант 2: Кастомный домен
```
https://api.ваш-домен.com
```

### Настройка переменной окружения для фронтенда

В настройках приложения Frontend на TimeWeb добавьте:

```env
VITE_API_URL=https://ваш-домен-api.timeweb.cloud
```

Или если используете кастомный домен:

```env
VITE_API_URL=https://api.ваш-домен.com
```

## 📝 Примеры использования

### Локально
```javascript
// frontend/src/services/api.js
baseURL: import.meta.env.VITE_API_URL || 'http://localhost:5001'
```

### В production
```javascript
// Автоматически использует VITE_API_URL из переменных окружения
baseURL: import.meta.env.VITE_API_URL
```

## 🔧 Настройка CORS

В `backend/GreenQuarter.Api/Program.cs` убедитесь, что в `AllowedOrigins` добавлен URL вашего фронтенда:

```csharp
var allowedOrigins = builder.Configuration["AllowedOrigins"]?.Split(',') 
    ?? new[] { 
        "http://localhost:5173", 
        "https://ваш-домен-frontend.timeweb.cloud",
        "https://ваш-домен.com"
    };
```

## ⚠️ Важно

1. **После деплоя бэкенда** получите его URL из панели TimeWeb
2. **Обновите переменную окружения** `VITE_API_URL` в настройках фронтенда
3. **Пересоберите фронтенд** после изменения переменных окружения
4. **Проверьте CORS** - добавьте URL фронтенда в `AllowedOrigins` бэкенда

