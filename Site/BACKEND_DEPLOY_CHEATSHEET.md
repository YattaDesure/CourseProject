# 🎯 Шпаргалка: Деплой бэкенда на TimeWeb

## Быстрая шпаргалка - что куда вводить

### 1. VCS провайдер
```
Тип: Git (или GitHub)
URL: https://github.com/YattaDesure/Site.git
Логин: (пусто)
Пароль: (пусто)
```

### 2. Создание приложения
```
Тип: Backend
Пресет: Docker
Реопозиторий: Site
Ветка: main
Коммит: последний (или 483e7e2588ba931fc82fb8322bff6274615d4d01)
```

### 3. Настройки сборки
```
Build context: backend/
Dockerfile: backend/Dockerfile
Порт: 8080
```

### 4. Переменные окружения (ОБЯЗАТЕЛЬНО!)

#### База данных:
```
DB_HOST = 0.tcp.ngrok.io (или ваш туннель)
DB_PORT = 12345 (порт из туннеля, обычно 1433 локально)
DB_NAME = GreenQuarter
DB_USER = sa
DB_PASSWORD = 22332123Yaz (ваш пароль)
```

#### JWT:
```
JWT_KEY = YourSuperSecretKeyThatIsAtLeast32CharactersLong!
JWT_ISSUER = GreenQuarter
JWT_AUDIENCE = GreenQuarterUsers
```

#### CORS (после деплоя фронтенда):
```
AllowedOrigins = https://ваш-домен-frontend.timeweb.cloud
```

---

## ⚡ Быстрый чек-лист

- [ ] VCS провайдер добавлен
- [ ] Backend приложение создано
- [ ] Build context: `backend/`
- [ ] Dockerfile: `backend/Dockerfile`
- [ ] Все переменные окружения добавлены
- [ ] Туннель к БД запущен (если БД на ноутбуке)
- [ ] Сборка запущена
- [ ] URL API скопирован

---

## 🔗 После деплоя

1. Скопируйте URL API (например: `https://greenquarter-api.timeweb.cloud`)
2. Добавьте в Frontend переменную: `VITE_API_URL=https://ваш-url-api.timeweb.cloud`
3. Пересоберите фронтенд

---

**Подробная инструкция:** `DEPLOY_BACKEND_STEP_BY_STEP.md`

