#!/bin/bash

# Скрипт инициализации базы данных
# Запускается после готовности SQL Server

SA_PASSWORD="${SA_PASSWORD:-22332123Yaz}"
SQL_SERVER="${SQL_SERVER:-sqlserver}"

echo "🔍 Проверка базы данных Cursovaya..."

# Проверяем, существует ли база данных
DB_EXISTS=$(/opt/mssql-tools18/bin/sqlcmd -S $SQL_SERVER -U SA -P "$SA_PASSWORD" -C -Q "SELECT name FROM sys.databases WHERE name = 'Cursovaya'" -h -1 2>/dev/null | grep -i cursovaya)

if [ -z "$DB_EXISTS" ]; then
    echo "📦 База данных не найдена, восстанавливаем из резервной копии..."
    
    # Проверяем наличие файла резервной копии
    if [ -f "/scripts/Cursovaya.bak" ]; then
        echo "📥 Найден файл резервной копии"
        echo "🔄 Восстановление базы данных из резервной копии..."
        
        # SQL Server может восстановить БД из файла, который находится в смонтированном volume
        # Файл доступен в SQL Server контейнере по пути /init-scripts/Cursovaya.bak
        /opt/mssql-tools18/bin/sqlcmd -S $SQL_SERVER -U SA -P "$SA_PASSWORD" -C \
            -Q "RESTORE DATABASE Cursovaya FROM DISK = '/init-scripts/Cursovaya.bak' WITH REPLACE"
        
        if [ $? -eq 0 ]; then
            echo "✅ База данных Cursovaya успешно восстановлена!"
        else
            echo "⚠️ Ошибка при восстановлении, пробуем альтернативный метод..."
            # Альтернативный метод: копируем файл через временный контейнер
            # Но проще создать пустую БД, если восстановление не удалось
            echo "⚠️ Создаем пустую базу данных..."
            /opt/mssql-tools18/bin/sqlcmd -S $SQL_SERVER -U SA -P "$SA_PASSWORD" -C \
                -Q "CREATE DATABASE Cursovaya"
            echo "✅ База данных Cursovaya создана (пустая)"
            echo "💡 Вы можете восстановить данные вручную позже"
        fi
    else
        echo "⚠️ Файл резервной копии не найден, создаем пустую базу данных..."
        /opt/mssql-tools18/bin/sqlcmd -S $SQL_SERVER -U SA -P "$SA_PASSWORD" -C \
            -Q "CREATE DATABASE Cursovaya"
        echo "✅ База данных Cursovaya создана"
    fi
else
    echo "✅ База данных Cursovaya уже существует"
fi

echo "🎉 Инициализация завершена"

