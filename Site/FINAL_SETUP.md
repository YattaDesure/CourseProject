# ✅ ФИНАЛЬНАЯ НАСТРОЙКА ДЛЯ TIMEWEB

## 🎯 Что сделано:

✅ Создан `Dockerfile` в корне проекта - TimeWeb автоматически его найдет!

## 📋 Настройки в TimeWeb:

### 1. Build context:
```
. (корень проекта)
```

### 2. Dockerfile:
```
Dockerfile
```
(или оставьте автоматический - TimeWeb найдет его сам)

### 3. Порт:
```
8080
```

## 🔧 Переменные окружения (ОБЯЗАТЕЛЬНО!):

Добавьте в разделе "Переменные окружения":

```
DB_HOST = адрес_из_ngrok (например: 0.tcp.ngrok.io)
DB_PORT = порт_из_туннеля (например: 12345)
DB_NAME = GreenQuarter
DB_USER = sa
DB_PASSWORD = ваш_пароль
JWT_KEY = YourSuperSecretKeyThatIsAtLeast32CharactersLong!
JWT_ISSUER = GreenQuarter
JWT_AUDIENCE = GreenQuarterUsers
```

## 💾 Закоммитьте Dockerfile в Git:

```bash
git add Dockerfile
git commit -m "Add Dockerfile for TimeWeb"
git push
```

## 🚀 После этого:

1. В TimeWeb нажмите "Создать" или "Деплой"
2. Дождитесь завершения сборки
3. Проверьте Swagger: `https://ваш-url-api.timeweb.cloud/swagger`

## ✅ Готово!

Dockerfile настроен правильно и должен работать на TimeWeb!

