# 🚀 Настройка нативного .NET деплоя на TimeWeb (без Docker)

## ✅ Решение: Использовать нативный .NET деплой

Если TimeWeb не позволяет выбрать Dockerfile, используйте нативный .NET деплой через форму настроек.

## 📋 Настройки в TimeWeb:

### 1. Окружение (Environment)
✅ **Выберите:** `.NET`

### 2. Фреймворк (Framework)
✅ **Выберите:** `ASP.NET`

### 3. Версия окружения (Environment Version)
✅ **Выберите:** `9.0`

### 4. Команда сборки (Build command)
**Введите:**
```bash
cd backend && dotnet restore GreenQuarter.sln && dotnet build GreenQuarter.Api/GreenQuarter.Api.csproj -c Release
```

**Или более простой вариант:**
```bash
dotnet restore backend/GreenQuarter.sln && dotnet build backend/GreenQuarter.Api/GreenQuarter.Api.csproj -c Release
```

### 5. Зависимости (Dependencies)
**Оставьте пустым** или укажите системные зависимости, если нужны.

### 6. Команда запуска (Run command)
**Введите:**
```bash
dotnet run --project backend/GreenQuarter.Api/GreenQuarter.Api.csproj --urls "http://0.0.0.0:8080"
```

**Или:**
```bash
cd backend/GreenQuarter.Api && dotnet run --urls "http://0.0.0.0:8080"
```

### 7. Путь проверки состояния (Health check path)
**Укажите:**
```
/swagger
```
Или оставьте пустым.

---

## 🔧 Переменные окружения (ОБЯЗАТЕЛЬНО!)

Добавьте все эти переменные в разделе "Переменные окружения":

### База данных:
```
DB_HOST = адрес_вашей_бд (из ngrok/cloudflared)
DB_PORT = порт_из_туннеля
DB_NAME = GreenQuarter
DB_USER = sa
DB_PASSWORD = ваш_пароль
```

### JWT:
```
JWT_KEY = YourSuperSecretKeyThatIsAtLeast32CharactersLong!
JWT_ISSUER = GreenQuarter
JWT_AUDIENCE = GreenQuarterUsers
```

### CORS (после деплоя фронтенда):
```
AllowedOrigins = https://ваш-домен-frontend.timeweb.cloud
```

---

## ⚠️ Важно:

1. **Порт:** Приложение должно слушать на `0.0.0.0:8080` (не `localhost`)
2. **Build context:** TimeWeb должен клонировать весь репозиторий
3. **Переменные окружения:** Все обязательные переменные должны быть заполнены

---

## 🎯 Альтернативные команды (если первые не работают):

### Команда сборки (вариант 2):
```bash
dotnet restore backend/GreenQuarter.sln
dotnet build backend/GreenQuarter.Api/GreenQuarter.Api.csproj -c Release
```

### Команда запуска (вариант 2):
```bash
cd backend/GreenQuarter.Api
dotnet run --urls "http://0.0.0.0:8080"
```

---

## 📝 Проверка после деплоя:

1. Откройте Swagger: `https://ваш-url-api.timeweb.cloud/swagger`
2. Проверьте логи на наличие ошибок
3. Проверьте подключение к БД

---

**Удачи с деплоем! 🚀**

