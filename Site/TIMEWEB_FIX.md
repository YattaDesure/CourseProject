# 🔧 Исправление проблемы с Dockerfile на TimeWeb

## Проблема

TimeWeb использует автоматически сгенерированный Dockerfile, который ищет `package.json` в корне проекта, но у нас он находится в `frontend/`.

## Решение 1: Настроить Build Context (Рекомендуется)

В настройках TimeWeb:

1. **Build Settings:**
   - **Build context:** `frontend/`
   - **Dockerfile path:** `frontend/Dockerfile`

Это заставит TimeWeb использовать правильный Dockerfile из папки frontend.

---

## Решение 2: Использовать Dockerfile из корня

Если TimeWeb требует Dockerfile в корне:

1. **Build Settings:**
   - **Build context:** `.` (корень проекта)
   - **Dockerfile path:** `Dockerfile`

Убедитесь, что файл `Dockerfile` в корне проекта (уже создан).

---

## Решение 3: Создать симлинк (если возможно)

Если у вас есть доступ к серверу по SSH:

```bash
cd /path/to/project
ln -s frontend/Dockerfile Dockerfile
```

---

## Решение 4: Переместить package.json (НЕ рекомендуется)

Если ничего не помогает, можно временно создать симлинк:

```bash
cd /path/to/project
ln -s frontend/package.json package.json
ln -s frontend/package-lock.json package-lock.json
```

Но это плохое решение, лучше использовать правильный build context.

---

## Проверка

После настройки проверьте:

1. Логи сборки в TimeWeb
2. Убедитесь, что используется правильный Dockerfile
3. Проверьте, что `package.json` находится в build context

---

## Если проблема остается

1. Проверьте настройки Build Context в TimeWeb
2. Убедитесь, что Dockerfile закоммичен в Git
3. Попробуйте использовать `Dockerfile.timeweb` (альтернативный вариант)

