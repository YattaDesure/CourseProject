# 🐳 Деплой с базой данных в Docker на ноутбуке

## Быстрый старт

### 1. Запустите Docker контейнер с SQL Server на ноутбуке

```bash
# Если используете docker-compose:
docker-compose up -d sqlserver

# Или если контейнер называется иначе, найдите его:
docker ps -a | grep sql

# И запустите:
docker start <имя-контейнера>
```

**Проверьте, что порт 1433 проброшен:**
```bash
docker ps | grep 1433
# Должно быть что-то вроде: 0.0.0.0:1433->1433/tcp
```

### 2. Установите и настройте ngrok

```bash
# Установка (macOS)
brew install ngrok

# Регистрация на ngrok.com и получение токена
ngrok config add-authtoken <ваш-токен>
```

### 3. Запустите туннель

```bash
# Простой способ:
ngrok tcp 1433

# Или используйте скрипт:
./start-db-tunnel.sh ngrok
```

Вы увидите:
```
Forwarding  tcp://0.tcp.ngrok.io:12345 -> localhost:1433
```

**Скопируйте адрес:** `0.tcp.ngrok.io:12345`

### 4. Настройте сервер TimeWeb

На сервере обновите строку подключения в systemd service:

```bash
sudo nano /etc/systemd/system/greenquarter-api.service
```

Используйте адрес из ngrok:
```ini
Environment=ConnectionStrings__DefaultConnection="Server=0.tcp.ngrok.io,12345;Database=Cursovaya;User Id=SA;Password=22332123Yaz;TrustServerCertificate=True;Encrypt=True;"
```

Перезапустите сервис:
```bash
sudo systemctl restart greenquarter-api
```

---

## Полный процесс работы

### На ноутбуке (каждый раз при работе):

1. **Запустите Docker контейнер:**
   ```bash
   docker-compose up -d sqlserver
   # или
   docker start <имя-контейнера>
   ```

2. **Запустите туннель:**
   ```bash
   ngrok tcp 1433
   # Скопируйте адрес (например: 0.tcp.ngrok.io:12345)
   ```

3. **Обновите адрес на сервере** (если адрес изменился):
   ```bash
   # На сервере TimeWeb
   sudo nano /etc/systemd/system/greenquarter-api.service
   # Обновите ConnectionStrings__DefaultConnection
   sudo systemctl restart greenquarter-api
   ```

### На сервере TimeWeb (один раз):

1. Настройте systemd service (см. DEPLOY_QUICKSTART.md)
2. Настройте Nginx для frontend
3. Обновите строку подключения с адресом ngrok

---

## Автоматизация (опционально)

### Скрипт для запуска всего сразу

Создайте `start-local-db.sh`:

```bash
#!/bin/bash

echo "🚀 Запуск базы данных и туннеля..."

# Запуск Docker контейнера
echo "1️⃣  Запуск Docker контейнера..."
docker-compose up -d sqlserver || docker start <имя-контейнера>

# Ждем запуска БД
echo "2️⃣  Ожидание запуска SQL Server..."
sleep 5

# Запуск туннеля
echo "3️⃣  Запуск ngrok туннеля..."
echo "⚠️  Скопируйте адрес из вывода ниже!"
echo ""
ngrok tcp 1433
```

Сделайте исполняемым:
```bash
chmod +x start-local-db.sh
```

---

## Проверка подключения

### На ноутбуке проверьте, что БД доступна:

```bash
# Проверка Docker контейнера
docker ps | grep sql

# Проверка порта
lsof -i :1433
```

### На сервере проверьте подключение:

```bash
# Если установлен sqlcmd
sqlcmd -S 0.tcp.ngrok.io,12345 -U SA -P 22332123Yaz -C -Q "SELECT @@VERSION"

# Или проверьте логи API
sudo journalctl -u greenquarter-api -f
```

---

## Важные замечания

1. **Адрес ngrok меняется** при каждом перезапуске (на бесплатном плане)
   - Решение: Используйте cloudflared для постоянного адреса
   - Или платный план ngrok

2. **Туннель должен быть запущен** пока работает сайт
   - Если туннель упадет, сайт не сможет подключиться к БД

3. **Docker контейнер должен быть запущен** на ноутбуке
   - Если ноутбук выключен, сайт не будет работать

4. **Безопасность:**
   - Используйте сильный пароль для SQL Server
   - Рассмотрите VPN вместо публичного туннеля для production

---

## Альтернатива: cloudflared (постоянный адрес)

Если нужен постоянный адрес без изменений:

```bash
# Установка
brew install cloudflared

# Запуск
cloudflared tunnel --url tcp://localhost:1433
```

Адрес будет постоянным для этого туннеля.

---

## Структура работы

```
┌─────────────────┐         ┌──────────────┐         ┌─────────────┐
│   Ноутбук       │         │    ngrok     │         │  TimeWeb    │
│                 │         │   (туннель)  │         │   Сервер    │
│  Docker SQL     │ ──────> │              │ ──────> │   API       │
│  Server:1433    │         │  0.tcp...    │         │             │
└─────────────────┘         └──────────────┘         └─────────────┘
```

1. Docker контейнер с SQL Server на ноутбуке (порт 1433)
2. ngrok создает туннель: `0.tcp.ngrok.io:12345 -> localhost:1433`
3. Сервер TimeWeb подключается через туннель к БД на ноутбуке

